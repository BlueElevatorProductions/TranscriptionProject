{
  "name": "podcast-transcriber",
  "version": "0.1.0",
  "description": "A cross-platform podcast transcript editor with integrated audio editing capabilities",
  "main": "build/main/main.js",
  "homepage": "./",
  "author": "BlueElevatorProductions",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "echo 'Run npm run dev:vite in one terminal and npm run dev:electron in another'",
    "dev:vite": "vite",
    "dev:electron": "electron .",
    "build": "npm run build:renderer && npm run build:main",
    "build:renderer": "vite build",
    "build:main": "tsc --project tsconfig.main.json",
    "start": "electron build/main/main.js",
    "clean": "rm -rf build dist node_modules",
    "postinstall": "echo 'Setup complete! Run npm run dev:vite then npm run dev:electron'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.45",
    "@types/react-dom": "^18.2.18",
    "@vitejs/plugin-react": "^4.3.3",
    "electron": "^28.1.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  }
}